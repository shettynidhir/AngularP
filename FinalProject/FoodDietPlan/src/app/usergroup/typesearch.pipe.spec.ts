import { TypesearchPipe } from './typesearch.pipe';

describe('TypesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new TypesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
